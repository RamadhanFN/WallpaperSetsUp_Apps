<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wallpapersetsup_apps">

    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TemaActivity" />
        <activity android:name=".WallpaperSlideshow" />
        <activity android:name=".PreviewSlide" />
        <activity android:name=".PreviewSlide2" />
        <activity android:name=".PreviewSlide3"/>

        <service android:name=".ServiceTime" />
        <service android:name=".ServiceTime2" />
        <service android:name=".ServiceTime3" />
    </application>

</manifest>